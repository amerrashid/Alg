import re
from .func_test import *

Input_list = [6, "g", 5, "b", "$", "c", 7, 26, 5, "z", "!"]

for i in Input_list:

    is_number = test_num(i)
    # is_char = test_char(i)
    # is_special = test_spec(i)

    # if is_number % 2 == 0:
    #     is_evennum = True
    #     if is_evennum:










